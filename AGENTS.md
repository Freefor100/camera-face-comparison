# 项目开发规则

## 项目阶段

这是尚未发布的个人课程设计项目，运行数据也处于开发阶段。

- 不兼容旧数据库、旧配置、旧接口和旧行为。
- 不添加迁移脚本、兼容分支、旧字段别名、回退解析或兼容性测试。
- 替换或删除功能时，同一改动中删除对应的代码、配置、数据结构、文档和测试。
- `data/` 下的开发数据可丢弃。数据库或配置结构变化后重新创建，不迁移旧数据。
- 完成清理前，使用 `rg` 搜索被删除的符号，并检查完整 diff，避免遗留死代码和过期说明。

## 范围与事实

- `design.md` 只描述当前已经实现的行为。
- `README.md` 必须分清当前能力、阶段 TODO 和系统局限性，不能把计划写成已完成。
- TODO 只保留课程时间内准备实现并验证的工作。论文级难题和当前不准备解决的问题写入系统局限性。
- 扩展功能必须有明确基线、同条件复测和结果记录；未实现的扩展不提前补测试。
- 源码、脚本和测试中的函数/类 docstring 使用中文，说明用途、输入、输出和必要条件；关键处理步骤使用中文注释。`noqa`、`type: ignore`、CSS 选择器和 SQL 字符串等机器语法标记不视为解释性注释。

## 测试

- 每个测试必须对应当前行为契约或已复现的问题。
- 文档修改只做文本、链接和 diff 检查，不运行 pytest。
- 小范围代码修改先跑受影响测试；跨模块里程碑结束时再跑一次全量测试。
- 不在每次编辑后重复运行全量测试，不测试已删除的兼容逻辑，不用脆弱的完整 UI 文案断言代替行为测试。
