# 人脸比对系统当前实现说明

本文只描述当前代码已经实现的行为。后续工作见 `README.md`，实验结论见
`docs/phase-5b-cross-quality-results.md` 和 `docs/phase-5c-results.md`。数据角色、识别指标和代码字段的定义见
`docs/术语与内部命名.md`。

## 1. 系统定位

程序是离线桌面端开放集 1:N 人脸识别系统：本地图片作为一张输入，摄像头点击识别时
采集 5 帧并选择其中一帧，先在标准库的所有身份中找第一候选，再用冻结的开放集规则决定
输出姓名还是“未知人员”。
它不是原图 1:1 像素比对，也不会因新增人员而重新训练 InsightFace。

运行时由以下模块组成：

- PySide6：界面、状态反馈和工作线程；识别页与标准库页分别由独立页面组件负责；
- OpenCV：跨平台摄像头、图片解码和图片保存；
- InsightFace `buffalo_l`：人脸检测、五点对齐和 512 维人脸特征向量；
- ONNX Runtime：优先使用 CUDA；模型不能在 CUDA 上运行时回退 CPU；
- SQLite：人员、样本人脸特征向量、原始质量指标和识别日志。

## 2. 摄像头与图片输入

摄像头由 OpenCV 按整数索引打开。Linux 优先 V4L2，Windows 优先 DirectShow，
macOS 优先 AVFoundation，平台后端失败时再使用 OpenCV 通用后端。Linux 索引 0
通常对应 `/dev/video0`，但程序不直接读取设备节点，也不写死某个路径。

预览由 `CameraWorker` 持续读取。每帧复制后交给用户界面，抓拍识别再复制一次，避免后台
推理读取不断变化的缓冲区。摄像头识别固定采集 5 帧，相邻采集间隔 80 ms；识别服务跳过
无效帧，并从有效帧中选择人脸区域拉普拉斯方差最高的一帧。具体做法是先按检测框裁出人脸、
转换为灰度图，再计算 `Var(Laplacian(face_crop))`；二阶边缘响应的方差越大，当前帧被视为越
清晰，相同时选择采集顺序更早的一帧。该指标只在同一次短时间采集的帧之间排序，不作为入库
或识别硬门，也不参与标准库相似度计算。选出一帧后，系统才用这一帧的人脸特征向量执行一次
标准库检索。预览运行时禁止刷新设备；停止后释放设备并清除当前帧、检测框和最后画面。本地
图片通过 `ImageInput.from_file()` 解码，只作为一帧输入进入同一个识别服务。

## 3. 人脸检测与特征提取

```text
BGR 图片
  → InsightFace 人脸检测
  → 恰好一张人脸？
       否：invalid（无人脸或多人脸）
       是：InsightFace 五点对齐与识别模型
  → 人脸特征向量有效且非零？
       否：invalid
       是：L2 归一化为单位向量
```

`FaceAnalysis` 只加载 `detection` 和 `recognition`，不运行应用没有使用的性别年龄与
额外关键点模型。使用两个独立空缓存对 XQLFW 同一批 7,263 张图片做优化前后对照后，
有效人脸特征向量的平均推理耗时从 32.293 ms 降到 23.896 ms；每张图片的处理状态、
质量指标和人脸特征向量均保持不变。

当前不按检测分、脸部尺寸、清晰度、亮度或对比度拒绝一张已经产生有效单脸
人脸特征向量的图片。原因是阶段 3 的 10,108 次单因素实验表明，旧启发式质量拒绝规则会
大量拒绝仍能正确识别的图片。程序仍记录五项原始指标，并把异常值转换为不阻断流程的
拍摄建议。

## 4. 标准库录入

创建人员支持一张或多张摄像头帧/本地图片，不要求固定姿态、动作或数量。每张输入必须
产生唯一且有效的人脸特征向量；任一输入失败时，本批录入整体失败。

```text
输入图片
  → 唯一人脸与人脸特征向量校验
  → 记录原始质量指标
  → 图片写入 data/faces/.staging/
  → 全部图片成功后移动到 data/faces/<person_id>/
  → 一个 SQLite 事务写入人员和全部样本
```

第一张有效样本写入后，人员立即参与识别。已有人员可继续原子追加多张样本。代码没有
空人员、人员生命周期、五姿态会话或旧数据库迁移逻辑。

## 5. 人员级表示与余弦相似度

设人员 (i) 有单位样本向量 (e_{i1},\ldots,e_{im})。当前固定使用人员平均特征向量：

\[
p_i=\operatorname{norm}\left(\frac{1}{m}\sum_{j=1}^{m}e_{ij}\right)
\]

输入单位向量 (q) 与该人员的分数为：

\[
S_i=q^Tp_i
\]

所以同一人员无论有一张还是多张样本，都只形成一个归一化的人员平均特征向量；识别时
待识别向量与每个人员平均特征向量各做一次点积。原始图片、质量指标和样本数量不直接
进入分数加权。

## 6. 开放集接收规则

所有人员分数按降序排列。当前唯一部署规则为：

```text
第一候选分数 >= 0.5557855367660522
    → matched / 输出第一候选姓名
否则
    → unknown / score_below_threshold
```

配置名称为 `minimum_score`。第一候选、第二候选及二者的 `score_gap` 仍写入日志并显示，
但候选分差不参与当前接收判定。库为空时返回 `unknown / empty_face_library`。

该方案来自 LFW/XQLFW 四个主要跨质量场景的参数选择集联合标定，不是主观默认值。
实验比较了固定首张参考图片、最高样本分数、人员平均特征向量，以及 K 分别取 2、3、5
的前 K 个样本分数平均；接收规则比较了最高分阈值、仅候选分差、双条件和邻域感知余弦
（NAC）。在参数选择集的四个主要场景都满足“有效未登记人员误接收率不超过 1%”后，
人员平均特征向量加最高分阈值获得最高的最差场景端到端正确接收率。NAC 使用 32 个相邻
身份时只增加 0.39 个百分点，未达到实验前规定的 1 个百分点收益门槛。

## 7. 识别服务顺序

主窗口启动时执行一次 SQLite、外键、样本图片哈希和人脸特征向量哈希检查；检查通过后从
SQLite 样本构建 `InMemoryFaceLibrary`。新增人员或追加样本成功后重复检查并刷新内存快照，
操作者也可以在标准库页手动复查。单次识别不再重复读取全部样本或执行完整性检查。

本地图片识别和摄像头多帧识别都由 `RecognitionWorker` 在后台线程调用
`RecognitionService`，服务按以下顺序执行：

1. 对一张本地图片，或摄像头采集的 5 帧逐帧提取唯一人脸和归一化人脸特征向量；
2. 跳过无人脸、多人脸或无效人脸特征向量，摄像头输入从有效帧中选择最清晰的一帧；
3. 测量检测分、脸部尺寸、清晰度、亮度和对比度，生成非阻断提示；
4. 从任务开始时取得的内存标准库快照中，用 `P × 512` 矩阵乘法计算所有身份分数；
5. 用 `minimum_score` 判断第一候选，得到 `matched` 或 `unknown`；
6. 写入一次识别日志，并把同一个结果对象交给用户界面。

`invalid` 表示图片解码、人脸数量、人脸特征向量或标准库完整性失败；`unknown` 表示已经
完成有效 1:N 检索，但最高身份分数未达到接收阈值。两者不能混为一类。

用户界面把实时画面和参与判定的抓拍画面分开显示。停止预览时立即清除最后一帧，实时画面区
恢复为黑色停止状态。识别结论只显示状态和姓名；成功、未知人员和无效输入分别使用绿色、红色
和橙色。最高相似度、固定阈值、候选分差、可选帧、耗时和判定依据分别写入固定字段，未识别时
显示 `--`；固定阈值使用独立紫蓝色，表示它来自已冻结的参数标定，而不是本次识别产生的数据。
标题右侧以单行显示推理后端、标准库人数和数据完整性状态，不再使用独立大卡片。

标准库人员列表使用单行姓名和样本数。人员详情不显示 `file`、`camera` 等内部来源代码，样本
只按顺序编号。缩略图保持原图宽高比，样本列数根据详情区宽度重新计算；窄窗口至少一列，单个
样本卡片不会横向铺满详情区。

## 8. SQLite 与简单完整性检查

当前数据库表：

- `persons(id, display_name, created_at)`；
- `face_samples(..., quality_metrics_json, source_type, image_sha256, embedding_sha256)`；
- `recognition_logs(..., top_score, second_score, score_gap, acceptance_score,
  acceptance_rule, latency_ms, reason)`。

SQLite 开启外键、WAL、5 秒 busy timeout 和 `BEGIN IMMEDIATE` 短写事务。SQLite 自身
负责并发写锁；应用不另外实现文件锁。入库时记录图片文件和人脸特征向量字节的 SHA-256，
程序启动、新增或追加样本成功后，以及操作者手动点击复查时检查文件缺失、误替换和 BLOB
损坏；单次识别不重复执行这项检查。该机制不是密码学防篡改：能同时改写数据和哈希的攻击者
不在本课设威胁模型内。

## 9. 实验基础设施

当前保留三类可复用实验组件：

- `RawEmbeddingCache`：按数据集、模型提取版本、相对路径和文件 SHA-256 保存原始
  人脸特征向量、指标、人脸特征提取失败与耗时；
- 连续候选分数导出：固定标准库和待识别图片后，保存多个聚合方法的候选身份与分数，
  但不提前应用接收阈值；
- 联合标定与评估：只用参数选择集的实际分数断点选参，再把冻结参数应用于一次
  独立验证集；支持按来源身份重复抽样计算 95% 置信区间，以及在小型标准库上重放。
- 生产性能回放：`scripts/benchmark_production_runtime.py` 直接调用真实 `RecognitionService`，
  记录单帧和五帧回放中的模型、质量、清晰度选择、矩阵检索、判定和日志阶段耗时；真实摄像头
  取帧与 Qt 屏幕合成不由该数据集回放代替。

XQLFW 官方图片对入口仍用于 1:1 跨质量验证；LFW/XQLFW 同路径变体用于开放集 1:N
联合标定。QMUL-SurvFace 的原始提取入口保留用于监控小脸域覆盖统计，但监控域旧判定器
已移除，避免把桌面阈值机械迁移成可部署结论。

## 10. 当前明确边界

- 不含活体检测、照片/屏幕攻击防护或支付级可信链；
- 不解决极暗、严重模糊、强遮挡、极端侧脸和远距离监控小脸；
- 不重新训练 InsightFace，不实现线性判别分析（LDA）、极值机（EVM）、学习式模板聚合
  或专用人脸图像质量评估模型；
- 质量提示阈值只是操作建议，不是识别准确率保证；
- 多帧只处理短时间内的偶发质量波动，不能恢复已经丢失的人脸信息；完整端到端耗时结果和
  多帧接入证据见 `docs/phase-5c-results.md`。
- 最终演示标准库已经建立，但不保证本机以外的真实摄像头已经完成现场验收；阶段 6 现场记录见
  `docs/phase-6-demo-acceptance.md`。
