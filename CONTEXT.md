# 人脸开放集识别领域词汇

本文统一项目中与人脸识别业务有关的称呼。代码字段和论文原文可以保留英文，但面向读者
的说明应优先使用这里确定的中文名称。

## 输入与标准库

**身份**：
标准库中一个可被识别的人。一个身份可以关联一张或多张参考图片。
_避免使用_：类别、用户（除非确实讨论系统账户）

**标准库**：
参与一次一对多身份识别的身份及其参考数据集合，英文文献常称 Gallery。
_避免使用_：图片数据库、训练集、Gallery（未解释时）

**参考图片**：
已明确归属于某个身份、用于计算该身份人员分数的人脸图片。
_避免使用_：标准照（本项目允许不同来源和质量）、Reference Sample（未解释时）

**待识别图片**：
本次送入系统识别的人脸图片，可以来自摄像头、本地文件或评测数据集，英文文献常称
Probe。
_避免使用_：探针、测试照、Probe（未解释时）

**已登记人员图片**：
真实身份存在于当前标准库，但该图片本身没有作为参考图片加入标准库的待识别图片。
_避免使用_：Known Probe（未解释时）

**未登记人员图片**：
真实身份不在当前标准库的待识别图片，用于检查系统能否拒绝未知人员。
_避免使用_：Unknown Probe（未解释时）

## 特征与识别结果

**人脸特征向量**：
人脸模型从一张对齐人脸中提取的数值表示，英文技术资料常称 embedding。
_避免使用_：编码、向量数据、embedding（未解释时）

**人员平均特征向量**：
同一身份全部单位人脸特征向量求平均后再次归一化得到的人员表示，代码和论文中称
Mean Prototype。
_避免使用_：缓存原型、平均模板、Mean Prototype（未解释时）

**人员分数**：
一张待识别图片与一个标准库身份之间的最终相似度分数。
_避免使用_：图片误差、向量模长、Person Score（未解释时）

**已识别**：
第一候选通过当前接收规则，系统输出对应的标准库身份。
_避免使用_：Matched（未解释时）

**未知人员**：
待识别图片已完成人脸特征提取和标准库检索，但第一候选没有通过接收规则。
_避免使用_：识别失败、Unknown（未解释时）

**无效输入**：
输入因无法解码、无人脸、多人脸或无法产生有效人脸特征向量而没有进入身份判定。
_避免使用_：低质量、未知人员、Invalid（未解释时）

## 评测分区

**参数选择集**：
只用于比较识别方法和选择参数的数据分区，英文评测协议常称 Calibration。
_避免使用_：训练集、Calibration（未解释时）

**独立验证集**：
参数固定后只用于检验泛化效果的数据分区，不能反过来继续调整参数，英文评测协议常称
Evaluation。
_避免使用_：测试集（未说明是否参与调参时）、Evaluation（未解释时）
