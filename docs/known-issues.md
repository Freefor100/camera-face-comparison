# 已知问题台账

本文件把已复现问题传递给有依赖关系的后续阶段。开始一个阶段前，读取“处理阶段”包含该阶段且尚未关闭的记录；达到关闭条件并补充可复现证据后才能关闭。
数据角色和指标定义见 [术语与内部命名](术语与内部命名.md)。

状态只使用 `待处理`、`已接受`、`已关闭`。

| ID | 状态 | 首次发现阶段 | 复现证据 | 影响 | 处理阶段 | 关闭条件 | 关闭证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ALG-001` | 已关闭 | 阶段 1 | 7 张有效待识别图片的小规模试运行和历史固定阈值报告随方法得到不同结果，证明同一初始阈值不能公平比较不同分数分布。 | 若不联合选择方法和参数，会把阈值差异误写成聚合方法优劣。 | 阶段 4/5B | 在同一参数选择集上联合比较六种聚合与接收规则，并只在独立验证集上报告固定方案。 | 阶段 5B 使用 205,344 条跨质量连续候选排序统一比较；在 1% 误接收率目标下选出人员平均特征向量 + 最高分阈值。详见 `docs/phase-5b-cross-quality-results.md`。 |
| `ALG-002` | 已关闭 | 阶段 2 | 历史扫描器曾用负阈值模拟仅候选分差，规则命名与实际逻辑不一致。 | 无法区分仅阈值、仅分差和真正双条件，也缺少邻域感知余弦规则对照。 | 阶段 5B | 四类规则使用明确类型且无哨兵值；在自然、跨质量待识别图片与混合标准库上用同一参数比较，并完成一次独立验证集。 | 已实现 `score_threshold`、`score_gap`、`score_and_gap`、`nac` 四种类型。1% 目标下双条件自动取 `G=0`，邻域感知余弦使用 32 个相邻身份时只提高最差场景正确接收率 0.39 个百分点，最终保留简单阈值。 |
| `ALG-004` | 已关闭 | 阶段 5 | 自然 LFW 的 1% 初步结果不能回答 XQLFW 待识别图片和质量不齐标准库。 | 尚未证明一套参数能同时覆盖主要图片质量域。 | 阶段 5B | 完整 XQLFW 13,233 张缓存和六场景报告生成；四个主要场景共同选参，压力场景单独报告。 | XQLFW 得到 13,113/13,233 个人脸特征向量；四主场景共同选出 `T=0.555786`，一次独立验证集最差已登记人员正确接收率 66.30%、最坏未知人员误接收率 1.43%，并保留置信区间和泛化风险。 |
| `ALG-003` | 已关闭 | 阶段 4 | 聚合与接收规则来自 4,599 身份标准库，可能无法迁移到课程演示的小标准库。 | 若规则依赖候选人数，3 人以上最终演示标准库可能出现明显分数漂移。 | 阶段 5B | 冻结参数后只在参数选择集身份中复核 3/5/10/25/50/100 人规模；不使用最终演示身份调参。 | 240 个小型标准库重放全部满足 1% 未知人员误接收率；3～50 人观测未知人员误接收率为 0，100 人最坏 0.0814%。摄像头继续只承担阶段 6 功能与展示验收。 |
| `QUAL-001` | 已关闭 | 阶段 2 | 旧启发式质量总分由检测置信度、尺寸、清晰度、亮度和对比度线性组合；历史质量加权的前 K 个样本分数平均在小规模试运行和固定阈值全量报告中均未优于人员平均特征向量。 | 质量分可能不代表对当前识别模型的真实效用，直接加权可能降低效果。 | 阶段 3 | 按质量分分组统计错误并绘制“减少错误数量—额外拒绝数量”曲线；无稳定关系则删除软加权，只保留原始指标或界面提示。 | 阶段 3 的 2,204 条待识别图片记录中，保留 99% 干净样本的阈值拒绝 1,055 条才移除仅有的 2 个错误，其中 1,053 条本来正确；标准库同样误拒 990 条。旧质量总分及其加权、分层运行时代码均已删除。详见 `docs/phase-3-results.md`。 |
| `QUAL-002` | 已关闭 | 阶段 2 | 阶段 3 的 10,108 条正式测量中，旧质量拒绝规则使待识别图片和标准库中“放行且第一候选正确”的数量分别由 2,202 降为 677 和 679；轻度模糊 σ=1 的待识别图片原始 116/116 正确，却被拒绝 112 张。 | 初始门槛明显过严，会把可以正确识别的输入挡在系统外。 | 阶段 5B 应用接入 | 删除质量拒绝门槛、启发式总分、质量加权和分层阈值；只让无人脸、多人脸、损坏图片或人脸特征提取失败阻断，其他指标仅提示。 | commit `0a34600`：运行时改为人员平均特征向量 + 单一最高分阈值；单脸的尺寸、清晰度或亮度指标较差时仍进入识别并返回原始指标和提示，相关行为测试通过。 |
| `ROB-001` | 已关闭 | 阶段 2 | 旧 XQLFW 入口沿用应用质量拒绝规则，7,263 张中只接受 2,296 张，6,000 对中仅 4 对完整有效，无法形成鲁棒性结论。 | 极小且被选择过的有效分母会把质量拒绝误当成跨质量识别效果。 | 阶段 5 | XQLFW 使用无质量预筛原始缓存和官方按折协议；与原始 LFW 在共同有效图片对上比较。 | CUDA 提取获得 7,200/7,263 张人脸特征向量和 5,894/6,000 个有效图片对；共同 5,871 对上准确率从 98.48% 降为 94.14%，同人相似度均值下降 0.2594。详见 `docs/phase-5-xqlfw-results.md`。 |
| `ROB-002` | 已接受 | 阶段 5 | QMUL-SurvFace 全量 242,453 张图片中只有 436 张得到人脸特征向量；3,000 个标准库身份仅 70 个可用，60,423 张同源待识别图片仅 4 张可评分且第一候选均错误。 | 当前模型无法直接用于远距离监控小脸域；极小有效分母不能给出稳定识别率。 | 阶段 5 接受为系统局限性 | 完整记录人脸特征提取失败、协议分母、有效分母和跨域规则全部拒绝；不把监控域专用训练、复原或域适配列入课设后续任务。 | 全量缓存 242,453 条且 SQLite 完整性正常；LFW 规则在 268 张可评分待识别图片上正确接收和错误接收均为 0，证实是全部拒绝而非鲁棒。详见 `docs/phase-5-qmul-results.md`。 |
| `EVAL-002` | 已关闭 | 阶段 3 | 阶段 2 缓存把质量拒绝结果与人脸特征向量放在同一策略键下，1,901 个被拒待识别图片没有保存原始人脸特征向量；另外应用多人脸规则使 457 个有标签 LFW 待识别图片被拒。 | 改质量策略仍会触发重提取，且数据集评测分母受到桌面用户界面业务规则污染。 | 阶段 4 | 建立只依赖模型/对齐的自然 LFW 原始缓存；有标签数据集使用确定性的主体脸选择；改变质量或判定参数不再重跑 InsightFace。 | CUDA 原始提取获得 13,185/13,233 张人脸特征向量，仅 48 张人脸特征提取失败；2,236 张多候选图片按最大主体脸保留。缓存复读为 13,233 次命中、0 次推理；阈值扫描直接复用 34,320 条分数。 |
| `PERF-001` | 已关闭 | 阶段 5 | XQLFW 原始提取中有效人脸特征向量平均耗时为 32.293 ms/图；人脸引擎还逐图执行应用未使用的性别年龄和额外关键点模型。 | 无用推理增加摄像头抓拍和批量实验等待时间。 | 阶段 5 | 只执行检测与识别模块，并在同一全量数据上证明输出不变且耗时下降。 | 独立空缓存重跑 7,263 张图后平均耗时为 23.896 ms/有效图，下降 26.0%；7,263 条状态、人脸数、质量指标和人脸特征向量 BLOB 零差异，验证准确率不变。详见 `docs/phase-5-xqlfw-results.md`。 |
| `PERF-002` | 待处理 | 阶段 5 | 当前识别服务每次都从 SQLite 读取全部样本、重新计算所有人员平均特征向量，再通过 Python 循环逐人比较。LFW 4,588 人实验中，该路径的延迟中位数为 50.86 ms，CPU 全量精确矩阵检索为 0.0524 ms。 | 演示小库影响很小，但标准库扩容后会产生可避免的重复计算；当前实验只测候选检索，不能代表完整应用耗时。 | 阶段 5 | 接入启动构建、变更后局部更新的内存标准库检索结构；新旧第一、第二候选及分数一致；完成含模型、完整性检查、判定和日志的完整链路对照。 | 待补。实验依据见 `docs/gallery-retrieval-benchmark.md`。 |
| `DATA-001` | 已关闭 | 阶段 1 | 250×250 LFW 曾被旧应用尺寸门全部拒绝。 | 质量预筛会污染有效分母，数据集像素域也不能直接代表桌面摄像头。 | 阶段 3/4 | 评测缓存不应用基于数值指标的质量预筛；应用是否按人脸尺寸强制拒绝由质量实验决定。 | 阶段 3 否定旧尺寸门的识别收益；阶段 4 原始缓存仅保留模型人脸特征提取失败；当前运行时只提示人脸偏小，不阻断有效人脸特征向量。 |
| `EVAL-001` | 已关闭 | 阶段 2 | 原评测缓存标识同时包含人脸特征向量提取参数和匹配阈值，调整阈值会产生新的缓存命名；原流式报告也不保存逐待识别图片连续分数。 | 阈值实验会错误触发昂贵的 InsightFace 重提取，且无法对相同分数做精确扫描。 | 阶段 2 | 提取标识与判定产物分离；cache-only 导出不初始化模型；分数库不含判定参数并覆盖全部当前有效待识别图片。 | 当前 `embedding_extraction_id` 只由模型/提取链决定；`decision_scores.sqlite` 和阶段 5B 排名库保存连续分数，修改接收参数只重算统计。 |
| `UI-001` | 已关闭 | 阶段 1 | `RecognitionResult` 已包含第二候选分数，但旧用户界面只显示最高相似度和耗时。 | 操作者看不到候选是否接近。 | 阶段 1 | 已识别和未知人员结果均显示候选分差；没有第二候选时显示 `--`。 | commit `8cad5fd`；对应用户界面行为测试通过。 |
| `UI-002` | 已关闭 | 阶段 1 | 预览运行时刷新设备会再次打开已占用的 `/dev/video0`，扫描失败后设备列表变空。 | 设备状态失真，影响停止后重开。 | 阶段 1 | 预览运行时禁止刷新和切换设备；停止后恢复。 | commit `a5cc738`；对应用户界面行为测试通过。 |

## 登记规则

- 崩溃、线程状态、摄像头打开、持久化和用户界面/服务不一致属于功能缺陷，稳定复现后做最小修复和针对性测试。
- 误拒、误识、聚合效果、质量拒绝规则和参数选择登记为 `ALG-*`、`QUAL-*` 或 `DATA-*`，不能用少量演示照片临时调参关闭。
- 新记录必须包含复现证据、影响、明确处理阶段和可验证关闭条件；“看起来好了”不是关闭证据。
