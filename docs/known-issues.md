# 已知问题台账

本文件把已复现问题传递给有依赖关系的后续阶段。开始一个阶段前，读取“处理阶段”包含该阶段且尚未关闭的记录；达到关闭条件并补充可复现证据后才能关闭。

状态只使用 `待处理`、`已接受`、`已关闭`。

| ID | 状态 | 首次发现阶段 | 复现证据 | 影响 | 处理阶段 | 关闭条件 | 关闭证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ALG-001` | 已关闭 | Phase 1 | 7 张有效 Probe pilot 和历史固定阈值报告随方法得到不同结果，证明同一初始阈值不能公平比较不同分数分布。 | 若不联合标定，会把阈值差异误写成聚合方法优劣。 | Phase 4/5B | 在同一 Calibration 上联合比较六种聚合与接收规则，并只在独立 Evaluation 上报告冻结方案。 | Phase 5B 使用 205,344 条跨质量无阈值排序统一比较；主工作点选出 Mean Prototype + 最高分阈值。详见 `docs/phase-5b-cross-quality-results.md`。 |
| `ALG-002` | 已关闭 | Phase 2 | 历史扫描器曾用负阈值模拟仅候选分差，规则命名与实际逻辑不一致。 | 无法区分仅阈值、仅分差和真正双条件，也缺少 NAC 对照。 | Phase 5B | 四类规则使用明确类型且无哨兵值；在自然、跨质量 Probe 与混合 Gallery 上用同一参数比较，并完成一次独立 Evaluation。 | 已实现 `score_threshold`、`score_gap`、`score_and_gap`、`nac` 四种类型。主目标下双条件退化为 `G=0`，NAC-32 只提高最差 TPIR 0.39 pp，最终保留简单阈值。 |
| `ALG-004` | 已关闭 | Phase 5 | 自然 LFW 的 1% 初步结果不能回答 XQLFW Probe 和质量不齐 Gallery。 | 尚未证明一套参数能同时覆盖主要图片质量域。 | Phase 5B | 完整 XQLFW 13,233 张缓存和六场景报告生成；四个主要场景共同选参，压力场景单独报告。 | XQLFW 得到 13,113/13,233 个 embedding；四主场景共同选出 `T=0.555786`，一次 Evaluation 最差 TPIR 66.30%、最坏 FPIR 1.43%，并保留置信区间和泛化风险。 |
| `ALG-003` | 已关闭 | Phase 4 | 聚合与接收规则来自 4,599 身份 Gallery，可能无法迁移到课程演示的小标准库。 | 若规则依赖候选人数，3 人以上 Demo Gallery 可能出现明显分数漂移。 | Phase 5B | 冻结参数后只在 Calibration 身份中复核 3/5/10/25/50/100 人规模；不使用最终 Demo 身份调参。 | 240 个小 Gallery 重放全部满足 1% FPIR；3～50 人观测 FPIR 为 0，100 人最坏 0.0814%。摄像头继续只承担 Phase 6 功能与展示验收。 |
| `QUAL-001` | 已关闭 | Phase 2 | 当前 `quality_score` 是检测置信度、尺寸、清晰度、亮度、对比度的启发式组合；历史质量加权 Top-K 在 pilot 和固定阈值全量报告中均未优于 Mean Prototype。 | 质量分可能不代表对当前识别模型的真实效用，直接加权可能降低效果。 | Phase 3 | 完成质量分分桶错误率、单调关系和 Error-versus-Reject 曲线；无稳定关系则删除软加权，只保留有证据的硬门或 UI 提示。 | Phase 3 的 2,204 条 Probe 记录中，99% Baseline 保留点拒绝 1,055 条才移除仅有的 2 个错误，其中 1,053 条本来正确；Gallery 同样误拒 990 条。已决定不让 `quality_score` 进入最终拒绝、加权或分层阈值。详见 `docs/phase-3-results.md`。 |
| `QUAL-002` | 待处理 | Phase 2 | Phase 3 的 10,108 条正式测量中，当前门使 Probe/Gallery 的正确 Rank-1 分别由 2,202 降为 677/679；轻度模糊 σ=1 的 Probe 原始 116/116 正确却拒绝 112 张。 | 初始门槛明显过严，会把可以正确识别的输入挡在系统外。 | Phase 5B 应用接入 | 删除数值质量硬门、启发式总分、质量加权和分层阈值；只让无人脸、多人脸、损坏图片或 embedding 失败阻断，其他指标仅提示。 | 待运行时重构与行为测试。 |
| `ROB-001` | 已关闭 | Phase 2 | 旧 XQLFW 入口沿用应用质量门，7,263 张中只接受 2,296 张，6,000 对中仅 4 对完整有效，无法形成鲁棒性结论。 | 极小且被选择过的有效分母会把质量拒绝误当成跨质量识别效果。 | Phase 5 | XQLFW 使用无质量预筛原始缓存和官方按折协议；与原始 LFW 在共同有效 Pair 上比较。 | CUDA 提取获得 7,200/7,263 张 embedding 和 5,894/6,000 个有效 Pair；共同 5,871 对上准确率从 98.48% 降为 94.14%，同人相似度均值下降 0.2594。详见 `docs/phase-5-xqlfw-results.md`。 |
| `ROB-002` | 已接受 | Phase 5 | QMUL-SurvFace 全量 242,453 张无质量门原始提取只有 436 张有效；3,000 个 Gallery 身份仅 70 个可用，60,423 张 Mated 仅 4 张可评分且 Rank-1 为 0。 | 当前模型无法直接用于远距离监控小脸域；极小有效分母不能给出稳定识别率。 | Phase 5 接受为系统局限性 | 完整记录 FTE、协议分母、有效分母和跨域工作点全拒绝；不把监控域专用训练、复原或域适配列入课设 TODO。 | 全量缓存 242,453 条且 SQLite 完整性正常；LFW 工作点在 268 张可评分 Probe 上 TPIR/FPIR 均为 0，证实是全拒绝而非鲁棒。详见 `docs/phase-5-qmul-results.md`。 |
| `EVAL-002` | 已关闭 | Phase 3 | Phase 2 缓存把质量拒绝结果与 embedding 放在同一策略键下，1,901 个被拒 Probe 没有保存原始 embedding；另外应用多人脸规则使 457 个有标签 LFW Probe 被拒。 | 改质量策略仍会触发重提取，且数据集评测分母受到桌面 UI 业务规则污染。 | Phase 4 | 建立只依赖模型/对齐的自然 LFW 原始缓存；有标签数据集使用确定性的主体脸选择；改变质量或判定参数不再重跑 InsightFace。 | CUDA 原始提取获得 13,185/13,233 张 embedding，仅 48 张 FTE；2,236 张多候选图片按最大主体脸保留。缓存复读为 13,233 次命中、0 次推理；阈值扫描直接复用 34,320 条分数。 |
| `PERF-001` | 已关闭 | Phase 5 | XQLFW 原始提取中有效 embedding 平均耗时为 32.293 ms/图；人脸引擎还逐图执行应用未使用的性别年龄和额外关键点模型。 | 无用推理增加摄像头抓拍和批量实验等待时间。 | Phase 5 | 只执行检测与识别模块，并在同一全量数据上证明输出不变且耗时下降。 | 独立空缓存重跑 7,263 张图后平均耗时为 23.896 ms/有效图，下降 26.0%；7,263 条状态、人脸数、质量指标和 embedding BLOB 零差异，验证准确率不变。详见 `docs/phase-5-xqlfw-results.md`。 |
| `DATA-001` | 已接受 | Phase 1 | 250×250 LFW 在应用默认 `min_face_size_px=112` 下无可用 Gallery；使用评测专用 80 像素后完成缓存。 | LFW 小图不能直接套用桌面摄像头正式尺寸门。 | Phase 1 接受；Phase 3/5 继续显式区分域 | 应用默认门槛不为适配数据集而静默降低；所有结果明确记录评测配置与数据域。 | Phase 2 manifest 记录 `min_face_size_px=80`，README 和数据集记录均区分应用配置。 |
| `EVAL-001` | 已关闭 | Phase 2 | 原评测缓存标识同时包含 embedding 提取参数和匹配阈值，调整阈值会产生新的缓存命名；原流式报告也不保存逐 Probe 连续分数。 | 阈值实验会错误触发昂贵的 InsightFace 重提取，且无法对相同分数做精确扫描。 | Phase 2 | 提取标识与判定标识分离；cache-only 导出不初始化模型；分数库不含判定阈值并覆盖全部当前有效 Probe。 | `embedding_extraction_id` / `decision_policy_id` 已拆分；`decision_scores.sqlite` 含 23,052 条分数和 1,901 条 Probe 拒绝；针对性测试验证阈值变化不使提取标识变化。 |
| `UI-001` | 已关闭 | Phase 1 | `RecognitionResult` 已包含第二候选分数，但旧 UI 只显示最高相似度和耗时。 | 操作者看不到候选是否接近。 | Phase 1 | Matched/Unknown 均显示候选分差；没有第二候选时显示 `--`。 | commit `8cad5fd`；对应 UI 行为测试通过。 |
| `UI-002` | 已关闭 | Phase 1 | 预览运行时刷新设备会再次打开已占用的 `/dev/video0`，扫描失败后设备列表变空。 | 设备状态失真，影响停止后重开。 | Phase 1 | 预览运行时禁止刷新和切换设备；停止后恢复。 | commit `a5cc738`；对应 UI 行为测试通过。 |

## 登记规则

- 崩溃、线程状态、摄像头打开、持久化和 UI/服务不一致属于功能缺陷，稳定复现后做最小修复和针对性测试。
- 误拒、误识、聚合效果、质量门和参数选择登记为 `ALG-*`、`QUAL-*` 或 `DATA-*`，不能用少量演示照片临时调参关闭。
- 新记录必须包含复现证据、影响、明确处理阶段和可验证关闭条件；“看起来好了”不是关闭证据。
