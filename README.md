# 摄像头人脸比对系统

离线、跨平台的开放集 1:N 人脸识别课程设计项目。程序从外置摄像头或本地图片读取人脸，在本地标准库中检索已录入人员；证据不足时输出“未知人员”，不会强行给出姓名。

当前版本已经完成主链路、质量验证、开放集规则联合标定、内存矩阵检索、多帧摄像头识别和界面收尾；剩余工作是阶段 6 的最终演示标准库与现场验收。当前代码的准确流程见 [design.md](design.md)，阶段 5C 的性能、多帧和界面证据见 [阶段 5C 结果](docs/phase-5c-results.md)，任务书技术要求见 [任务书要求提取](docs/任务书要求提取.md)，需求分析见 [需求分析](docs/需求分析.md)，术语见 [术语与内部命名](docs/术语与内部命名.md)，跨质量 1:N 的完整实验和参数选择见 [阶段 5B 结果](docs/phase-5b-cross-quality-results.md)，论文与标准依据见 [开放集识别与质量评估调研](docs/技术调研-开放集识别规则与质量评估.md)。

## 项目目标

- 调用外置摄像头，显示实时画面并支持抓拍。
- 建立不少于 3 个身份的标准人脸库。
- 对输入图片完成人脸检测、特征提取和开放集 1:N 识别。
- 匹配成功时显示姓名和相似度，不满足规则时显示“未知人员”。
- 通过图形界面从摄像头或本地图片新增人员、追加样本。
- 保留“发现问题—实施优化—同条件复测”的量化过程，按开发计划完成可展示、可量化的鲁棒性和端到端时间优化。

## 任务书完成度

| 任务书要求 | 当前状态 | 完成度判断 |
| --- | --- | --- |
| 外置摄像头实时采集与拍照 | 已实现跨平台设备扫描、预览线程、停止清屏和抓拍入口 | **阶段 1 真实硬件链路已通过** |
| 不少于 3 个身份的标准库 | 已实现人员、图片和人脸特征向量的持久化；尚未建立最终演示标准库 | **代码已实现；最终数据待阶段 6** |
| 姓名/未知人员判别 | 已接入跨质量联合标定选出的人员平均特征向量 + 最高分阈值 | **算法、参数和运行时代码已统一** |
| 系统图形界面 | 已实现深色双栏识别页、标准库搜索/详情页、状态和异常反馈 | **阶段 5C 已完成；仍需阶段 6 现场验收** |
| 标准库动态扩容 | 已实现本地图片和当前画面新增、追加及重启恢复 | **阶段 1 完整链路已通过** |
| 扩展优化功能 | 已完成未使用模型裁剪、内存矩阵检索、完整链路对照和多帧清晰度择优 | **阶段 5C 已完成；结果与边界见阶段 5C 文档** |
| 优化前后对比证据 | 已完成质量实验、六种聚合方法联合标定、XQLFW/QMUL 压力实验、完整链路和多帧同条件对照 | **阶段 2～5C 已形成阶段证据** |

“代码已实现”只说明相应路径存在且自动化测试通过，不等于已经完成真实摄像头、正式数据集或现场条件验收。

## 当前实现

- 以 Python 源码项目运行，不制作 EXE；推荐 Python 3.11–3.13。
- PySide6 提供桌面图形界面，OpenCV 负责摄像头和图片读写。
- InsightFace `buffalo_l` 与 ONNX Runtime 提取人脸特征向量；Linux 上自动优先使用 CUDA，实际模型不能在 CUDA 上运行时回退 CPU。模型从本地 `data/models/` 加载。
- 摄像头帧和本地图片使用同一套检测、特征提取、原始质量测量和识别服务。
- 创建人员时必须同时提供至少一张有效图片；人员和首批样本原子写入后立即参与识别。没有固定姿态、动作或图片数量要求。
- 同一人员可以继续追加多张样本。每个人员的全部单位向量先求平均，再归一化为一个人员平均特征向量；待识别向量随后与所有人员平均特征向量做一对多余弦相似度比较。
- SQLite 保存人员、人脸特征向量、质量元数据和识别日志；样本原图保存在 `data/faces/`。
- 图片与向量分别记录 SHA-256，用于发现文件缺失、误覆盖或向量 BLOB 被改写。
- 当前唯一部署规则是 `人员平均特征向量 + score_threshold`，`minimum_score=0.5557855`；候选分差只展示和记录，不参与接收判定。
- 程序启动时从 SQLite 样本构建只读的人员平均特征矩阵；识别任务使用矩阵快照做 CPU 精确检索，新增或追加样本后刷新快照，不把派生平均向量另存为数据库字段。
- 摄像头点击识别时采集 5 帧、相邻间隔 80 ms，选择人脸区域最清晰的一帧参与判定；本地图片仍按单帧处理。
- 阶段 2 的 3,842 张有效待识别图片结果保留为“质量预筛为何会污染实验”的历史诊断。阶段 4 已重建与质量策略无关的 13,185 张自然 LFW 人脸特征向量，并保存 5,720 张有效待识别图片在六种聚合方法下、尚未应用接收规则的候选分数。
- 阶段 3 已完成 10,108 条正式单因素质量测量。结果证明旧启发式质量拒绝规则严重过严，旧质量总分不应作为主算法的拒绝、加权或分层阈值依据；当前只记录原始指标并给出非阻断提示。
- 阶段 4 的自然 LFW 结果中曾出现仅候选分差方案；该历史结果只用于说明联合标定的必要性，不代表当前部署规则。最终部署方案以阶段 5B 的跨质量联合标定为准。
- 阶段 5 的 XQLFW 全量实验已完成：共同有效 5,871 对上，原始 LFW 到跨质量变体的 10 折验证准确率由 98.48% 降至 94.14%，同人相似度均值下降 0.2594。
- 人脸引擎已停止执行未使用的性别年龄和额外关键点模型；同一批 7,263 张 XQLFW 图片上，有效人脸特征向量平均耗时由 32.293 ms 降至 23.896 ms，结果逐字节不变。该结果是推理子阶段优化，不等同于端到端优化。
- 阶段 5 的 QMUL-SurvFace 全量压力实验已完成：242,453 张监控图片中仅 436 张产生人脸特征向量，3,000 个标准库身份仅 70 个可用；这被记录为监控小脸域限制，不用于修改桌面阈值。
- 阶段 5B 已补齐 13,233 张 XQLFW 同路径人脸特征向量，生成六个场景、六种聚合方法的 205,344 条候选排序；这些排序尚未应用接收参数，可直接比较最高分阈值、仅候选分差、双条件和邻域感知余弦（NAC）规则。
- 以“参数选择集中四个主要场景的有效未登记人员误接收率均不超过 1%”为约束时，最终选出 `人员平均特征向量 + score_threshold`，`minimum_score=0.5557855`。NAC 使用 32 个相邻身份时，最差场景的已登记人员正确接收率只高 0.39 个百分点，未达到实验前规定的 1 个百分点收益门槛。
- 冻结参数在参数选择集的 3～100 人小型标准库中完成 240 次重放，全部满足 1% 未知人员误接收率；随后只执行一次独立验证集，最差跨质量已登记人员正确接收率为 66.30%，最坏未知人员误接收率为 1.43%。完整计数和置信区间见阶段 5B 结果。
- 已用真实 LFW 图片完成 [标准库精确矩阵检索和完整链路对照](docs/phase-5c-results.md)：4,588 人时，完整链路中位数由 566.103 ms 降至 288.430 ms；含启动成本的中位数为 320.000 ms。3、10、100 人小库收益很小，详细分阶段耗时和等价性证据见阶段 5C 文档。

## 阶段 TODO

### 阶段 0：录入规则清理（已完成）

- [x] 人员记录与至少一张有效样本原子创建，创建成功后立即参与识别。
- [x] 删除固定姿态流程和多张样本激活门槛。
- [x] 删除空人员状态字段、旧数据库迁移和对应旧接口、测试、文档。
- [x] 配置文件只接受当前完整结构，不解析旧字段或为缺失字段回退。
- [x] 修正任务书提取和当前实现说明。

### 阶段 1：基础链路验收（已完成）

验收过程见 [阶段 1 基础链路验收记录](docs/phase-1-validation.md)，跨阶段问题由 [已知问题台账](docs/known-issues.md) 传递和关闭。

- [x] 建立可随时删除的开发标准库，不导入最终展示人员。
- [x] 验证外置摄像头扫描、连续采集、停止清屏、重开和摄像头来源录入。
- [x] 验证单张/多张本地图片录入、摄像头来源录入和动态追加。
- [x] 验证已登记人员、未知人员、低质量输入、多人脸和系统错误提示。
- [x] 验证关闭数据库后重开，人员、样本、完整性状态和识别链路仍然正常。
- [x] 记录问题清单；这些临时身份和结果不进入最终验收数据。

### 阶段 2：固定评测数据和连续候选分数（已完成）

- [x] 使用 LFW 全量源协议固定标准库、已登记人员图片和未登记人员图片。
- [x] 在质量过滤前按身份固定参数选择集/独立验证集；同一已登记人员或未知人员来源身份不跨分区。
- [x] 把模型提取标识、原始人脸特征向量缓存和后续判定产物分离，质量与接收参数变化不再触发模型重提取。
- [x] 从现有 8,577 条有效缓存生成 3,842 张有效待识别图片的连续候选分数，不重新运行 InsightFace。
- [x] 保存固定首张参考图片、最高样本分数、人员平均特征向量，以及 K 分别取 2、3、5 的前 K 个样本分数平均，共 23,052 条第一、第二候选分数。
- [x] 保存 1,901 条待识别图片与 2,755 条标准库历史拒绝记录、协议哈希和代码版本，为后续发现质量预筛污染提供证据。
- [x] 将固定阈值全量结果降级为历史诊断，不据此选择最终方法。
- [x] 固化开放集识别、人员多样本聚合、人脸图像质量评估和大规模检索的技术调研及项目简化边界。

### 阶段 3：质量拒绝规则与入库拒绝规则验证

- [x] 将原始质量测量与质量拒绝规则拆开，为后续删除无依据的拒绝门槛保留连续分数证据。
- [x] 固定参数选择集中全部 116 个有效已登记人员身份和种子选择的 300 个未知人员身份；协议不含独立验证集，身份和图片均无交集或缺失。
- [x] 分别改变待识别图片和入库参考图，保存可恢复的原始人脸特征向量与质量指标。
- [x] 单因素测试人脸尺寸、模糊、亮度和对比度，不制造全因素笛卡尔组合。
- [x] 记录人脸特征提取失败、质量拒绝率、同人相似度、第一候选身份正确率、未知人员最高候选分数分布和低质量参考图影响；最终未知人员误接收率留到阶段 4 参数组合统计。
- [x] 从基线实际指标断点比较质量拒绝门槛的候选值，量化“错误减少量”和“额外拒绝量”。
- [x] 按质量总分分组统计错误，并绘制“减少错误数量—额外拒绝数量”曲线，确认旧质量总分的拒绝代价远大于收益。
- [x] 冻结算法结论：没有实验证据支持的质量拒绝门槛、软加权和分层阈值不进入最终主算法；只有解码失败、无人脸、多人脸或无效人脸特征向量阻断。

### 阶段 4：聚合方法与开放集判定联合标定

- [x] 在 CUDA 上重建与启发式质量策略无关的 13,185 张自然 LFW 人脸特征向量；数据集有标签时选择主体脸，只保留 48 张模型人脸特征提取失败。
- [x] 生成 5,720 张有效待识别图片在六种聚合方法下的 34,320 条连续候选分数，并验证再次读取 13,233 张缓存记录时不触发模型推理。
- [x] 在相同参数选择集上比较六种聚合方法，各自包含“仅匹配阈值”和“匹配阈值 + 候选分差”，共 12 组。
- [x] 使用实际 `top_score` / `score_gap` 断点精确扫描，不使用反向传播或固定网格。
- [x] 分别报告未知人员误接收率不超过 1%、不超过 0.3%，以及参数选择集中观测误接收为 0 时的结果；该阶段把 0.3% 作为主要约束。
- [x] 按预定规则选出 `人员平均特征向量 + score_gap`，并只在独立验证集执行一次最终统计。
- [x] 独立验证集得到第一候选身份正确率 98.60%、未知人员误接收率 0.252%、已登记人员正确接收率 48.45%；完整分母、参数和局限见阶段 4 结果。
- [x] 自然 LFW 阶段结果保留为历史基线，由阶段 5B 跨质量联合标定决定最终部署候选。

### 阶段 5：鲁棒性扩展、耗时与用户界面收尾

- [x] 使用 XQLFW 官方 6,000 对完成真实跨质量分析，并与原始 LFW 的同协议、共同有效图片对比较。
- [x] 删除逐图执行的未使用模型，并用独立空缓存全量复测：有效人脸特征向量平均耗时下降 26.0%，7,263 条结果零差异。
- [x] 完整处理 QMUL-SurvFace 242,453 张官方图片，记录原始模型覆盖、人脸特征提取失败、可评分分母，以及直接套用 LFW 规则后全部拒绝的现象；将监控小脸域列为系统限制。
- [x] **阶段 5B：**补齐 XQLFW 13,233 张同路径人脸特征向量，构造自然、跨质量待识别图片、混合质量标准库和全低质量压力场景。
- [x] **阶段 5B：**显式比较最高分阈值、仅候选分差、真正双条件和邻域感知余弦规则；在四个主要场景共同选择一套参数。
- [x] **阶段 5B：**在参数选择集内完成 3～100 人稳定性检查，冻结 `人员平均特征向量 + score_threshold, T=0.5557855` 后只运行一次独立验证集。
- [x] 把阶段 5B 冻结的人员平均特征向量和明确接收策略接入桌面应用，删除旧质量加权的前 K 个样本分数平均、分层阈值和基于数值指标的质量强制拒绝。
- [x] 使用真实 LFW 人脸特征向量比较当前重复计算路径、CPU 连续矩阵和 CUDA `MatMul+TopK`，验证 CPU 全量精确矩阵比较更适合当前单张图片识别，并记录构建耗时、内存占用和结果一致性。
- [x] 把 CPU 全量精确矩阵比较接入桌面程序：启动时从 SQLite 样本构建内存标准库；录入成功后刷新快照；不把人员平均特征向量重复存入数据库。
- [x] 用同一批真实 LFW 图片验证内存矩阵与逐人基线的第一、第二候选、分数和接受结果一致，再删除识别热路径中的重复读取与逐人求平均。
- [x] 记录图片输入、人脸检测、特征提取、质量测量、候选检索、判定、日志、线程和界面更新的分阶段耗时，并验证完整性检查移出每次识别后大规模标准库的完整链路收益。
- [x] 实现“短时间多帧采集 + 清晰度择优”，在身份互斥独立验证集上确认最差场景收益和未知人员误接收率后接入摄像头识别。
- [x] 完成用户界面视觉、状态反馈、标准库搜索/详情、操作说明和故障排查收尾；离屏检查五种关键状态。
- [x] 完成阶段 5C 复测；模型、输入校验、聚合方法和判定参数保持阶段 5B 的冻结版本。

### 阶段 6：最终演示标准库与验收

- [ ] 参数冻结后再导入“本人 + 少量公开身份”。
- [ ] 标准库至少包含 3 个身份；具体数量在本阶段根据展示需要决定。
- [ ] 本人站到摄像头前演示已登记人员，同学临时测试未知人员。
- [ ] 演示动态新增身份、追加样本、重启恢复和离线运行。

LFW 用于固定开放集协议、质量验证和自然域基线；XQLFW 与同路径 LFW 一起参与阶段 5B 跨质量统一选参；QMUL 只负责监控小脸域压力和系统局限性。摄像头不承担大规模调参，只用于多帧扩展的最终功能复验与现场展示。最终演示标准库要等运行时规则和用户界面冻结后再建立。

## 可实现优化与系统局限性

扩展功能是课程加分项。本项目开发计划包含两个方向：一是“短时间窗口多帧采集 + 质量择优”，二是面向完整识别链路的端到端时间优化。前者的目标是减少单帧偶发模糊、眨眼或曝光波动导致的拒识；后者以实际端到端耗时为依据定位瓶颈并实施小范围优化。两者都必须保留优化前基线、同条件复测结果、收益和代价。多帧质量择优不声称解决极端暗光、严重模糊、强遮挡或极端侧脸等信息已经丢失的情况；时间优化也不以牺牲识别结果或异常处理为代价。这类质量评估和多样本聚合方法有明确的研究依据，例如 [Quality Aware Network](https://openaccess.thecvf.com/content_cvpr_2017/html/Liu_Quality_Aware_Network_CVPR_2017_paper.html)、[SER-FIQ](https://openaccess.thecvf.com/content_CVPR_2020/html/Terhorst_SER-FIQ_Unsupervised_Estimation_of_Face_Image_Quality_Based_on_Stochastic_CVPR_2020_paper.html) 和 [MagFace](https://openaccess.thecvf.com/content/CVPR2021/html/Meng_MagFace_A_Universal_Representation_for_Face_Recognition_and_Quality_Assessment_CVPR_2021_paper.html)。课设只采用可解释的工程简化，不复现这些论文的训练方法或效果。

下面的问题不放进 TODO，也不承诺在课设中解决；最终报告会把它们作为系统局限性，并保留失败样例：

- 极暗环境中脸部信号已经丢失，或严重运动模糊、强遮挡、极端侧脸导致关键身份信息不可见。
- 远距离小脸、低分辨率监控画面和摄像头跨域泛化。QMUL-SurvFace 针对的正是更困难的监控小脸场景，与桌面 UVC 摄像头展示条件不同。
- 支付级安全，包括活体检测、照片/屏幕重放攻击防护、传感器可信链和攻击者模型。本项目的 SHA-256 只发现误删、误替换和局部损坏，不是安全认证机制。
- 对所有相机、肤色、年龄、光照和姿态给出统一准确率保证。当前原始质量指标只用于操作
  提示，不是经过大规模人群与设备标定的人脸图像质量评估模型。

多帧择优可以降低偶发眨眼、轻度模糊和单帧曝光波动的影响，但不能恢复已经丢失的图像信息。XQLFW 可用于观察跨质量退化，[QMUL-SurvFace](https://arxiv.org/abs/1804.09691) 可用于压力测试；它们都不能替代与实际摄像头条件接近的参数选择集和最终现场验收。

## 安装

Windows、Linux、macOS 均使用独立虚拟环境。必须明确选择 CPU 或 GPU 后端，不能同时安装两个 ONNX Runtime 包。

Linux/macOS 使用 CPU：

```bash
python3.11 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e ".[cpu,dev,evaluation]"
```

Linux（NVIDIA GPU）使用 GPU：

```bash
python -m pip install -e ".[gpu,dev,evaluation]"
```

程序启动时会检查 InsightFace 模型的实际执行后端；评测 JSON 中的 `execution_backend`
字段是最终依据，不能只根据 `nvidia-smi` 命令成功或可用后端列表判断模型是否真的使用了 GPU。

Windows PowerShell 激活命令：

```powershell
.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[cpu,dev,evaluation]"
```

在能联网的开发机器准备一次模型：

```bash
python scripts/prepare_models.py --data-dir ./data
```

脚本把模型放入可搬运的 `data/models/`。应用启动和正常演示只读取本地模型，不主动联网；演示机器需要复制源码和完整 `data/` 目录。

## 启动与操作

```bash
python -m camera_face_comparison --data-dir ./data
```

### 实时比对

1. 刷新设备并选择摄像头，然后启动预览。
2. 点击“抓拍并比对”，或点击“选择本地图片”。
3. 查看姓名/未知人员、相似度、判定原因和处理耗时。
4. 停止预览后，程序清空当前帧和最后显示画面。

### 标准人脸库

- “从本地图片新增人员”：输入姓名后选择一张或多张图片。
- “从当前画面新增人员”：把当前摄像头帧作为首张样本。
- 第一张能够产生唯一有效人脸特征向量的图片保存成功后，人员立即参与识别。
- “为选中人员导入图片”或“添加当前画面”可以继续追加样本。
- 多张样本可改善覆盖范围，但不是录入门槛，也不要求固定动作。

## 当前识别规则

系统不做原图像素级比较。输入人脸和标准库样本都被转换为 L2 归一化特征向量，余弦相似度为：

```text
sim(q, e) = q · e
```

当前实现对每个人员执行以下操作：

1. 对该人员的全部单位样本向量求平均，再归一化为一个人员平均特征向量。
2. 待识别向量与每个人员平均特征向量计算一次余弦相似度。
3. 对所有身份排序，得到第一候选、第二候选和候选分差。
4. 只检查第一候选分数是否达到联合标定阈值。

当前接收条件为：

```text
第一候选分数 >= minimum_score（当前为 0.5557855367660522）
```

达到阈值时输出第一候选姓名，否则输出“未知人员”。无人脸、多人脸、损坏图片和无效人脸特征向量不进入身份打分；尺寸、模糊、亮度、对比度只生成拍摄建议，不会伪装成未知人员或直接拒绝。

## 数据目录与一致性检查

```text
data/
├─ config.toml                    # 冻结识别策略和非阻断提示界限
├─ face_library.sqlite            # 人员、向量、原始质量指标、识别日志
├─ faces/<person-id>/             # 已入库样本图片
├─ demo-candidates/<person-id>/   # 最终演示候选，当前不进入数据库
├─ models/buffalo_l/              # 离线 ONNX 模型
├─ datasets/                      # 开发数据集、协议（可选）
├─ experiments/phase2/            # 历史质量预筛分数
├─ experiments/phase3/            # 单因素质量实验
├─ experiments/phase4/            # 自然 LFW 连续候选分数与历史基线
├─ experiments/phase5b/           # 跨质量联合标定和一次独立评估
└─ logs/                          # 评分记录和实验报告
```

SQLite 开启外键、WAL、busy timeout 和短写事务。人员与一批样本的创建使用同一个数据库事务；图片先写入 staging 目录，全部成功后再进入正式目录。项目不兼容旧数据库和旧配置；结构变化后删除开发用 `face_library.sqlite` 与过期 `config.toml`，由程序按当前结构重新创建。

SHA-256 检查针对已入库参考图片和 SQLite 中的人脸特征向量 BLOB，不检查摄像头当前帧，也不参与相似度计算。它用于发现误删、误替换和局部数据损坏，不用于抵御能够同时改写图片、向量和哈希值的攻击者。

`data/` 被 Git 忽略，可能包含人脸图片、模型和实验数据。

## 当前数据集评测入口

当前保留的是可复用的“原始人脸特征向量 → 尚未应用接收规则的候选分数 → 在参数选择集选择规则和阈值 → 固定参数后只运行一次独立验证集”链路。早期固定阈值、质量预筛和机械跨域判定脚本已经删除，历史结果仍保留在阶段文档和 `data/experiments/`。所有逐图产物均在被 Git 忽略的 `data/` 下。

LFW 全量协议与自然图片原始缓存：

```bash
python scripts/prepare_lfw.py --data-dir ./data --full \
  --known-fraction 0.8 --enrollment-per-identity 5 --seed 2026 \
  --output ./data/datasets/lfw_full_open_set_protocol.json
python scripts/extract_lfw_raw_embeddings.py --data-dir ./data
python scripts/export_lfw_decision_scores.py \
  --data-dir ./data \
  --source-protocol ./data/datasets/lfw_full_open_set_protocol.json \
  --cache-path ./data/logs/cache/lfw_raw.sqlite \
  --output-dir ./data/experiments/phase4
```

阶段 5B 使用完整 XQLFW 同路径变体，并在一次独立验证集前先完成参数选择集与小型标准库门槛：

```bash
python scripts/extract_xqlfw_raw_embeddings.py --data-dir ./data \
  --protocol ./data/experiments/phase4/protocol.json \
  --cache-path ./data/logs/cache/xqlfw_full_cuda.sqlite \
  --manifest ./data/experiments/phase5b/xqlfw_full_cuda_manifest.json
python scripts/export_cross_quality_scores.py --data-dir ./data \
  --xqlfw-cache ./data/logs/cache/xqlfw_full_cuda.sqlite
python scripts/calibrate_cross_quality.py \
  --scores ./data/experiments/phase5b/decision_scores.sqlite
python scripts/evaluate_gallery_scale.py --data-dir ./data \
  --xqlfw-cache ./data/logs/cache/xqlfw_full_cuda.sqlite
python scripts/evaluate_cross_quality_policy.py \
  --scores ./data/experiments/phase5b/decision_scores.sqlite \
  --calibration-report ./data/experiments/phase5b/calibration_report.json
```

XQLFW 官方 6,000 图片对验证和 QMUL 原始模型覆盖压力提取：

```bash
python scripts/extract_xqlfw_raw_embeddings.py --data-dir ./data
python scripts/evaluate_xqlfw.py --data-dir ./data
python scripts/compare_xqlfw_domains.py
python scripts/prepare_qmul.py --dataset-root ./data/datasets/qmul-survface/QMUL-SurvFace
python scripts/extract_qmul_raw_embeddings.py --data-dir ./data \
  --dataset-root ./data/datasets/qmul-survface/QMUL-SurvFace
```

当前部署规则和一次性独立验证集结果已经固定，不再用独立验证集反复调参。阶段 5C 已完成内存矩阵检索、完整链路性能对照、多帧清晰度择优和界面收尾；下一步只建立最终演示标准库并进行现场验收。

## 测试

```bash
QT_QPA_PLATFORM=offscreen python -m pytest -q
python -m ruff check .
python -m compileall -q src scripts tests
```

自动化测试不访问网络、个人照片、本地模型或真实摄像头。公开数据集实验由独立命令行脚本和 JSON/SQLite 产物验收；最终演示标准库与现场图形界面在阶段 6 单独验收。

## 摄像头故障排查

- Linux：`ls /dev/video*` 查看系统识别的设备，并确认当前用户有视频设备权限。程序把设备索引交给 OpenCV，Linux 优先使用 V4L2；不自行读取 `/dev/video*` 字节流，也不把设备路径写死为 `/dev/video0`。节点存在不代表一定能提供采集画面，例如同一 UVC 设备可能同时暴露视频节点和非采集节点。
- Windows：在隐私设置中允许桌面应用访问摄像头；程序优先 DirectShow，失败时回退 OpenCV 通用后端。
- macOS：在“隐私与安全性”中授予终端或 Python 摄像头权限；程序优先 AVFoundation。
- 模型缺失：在有网络的机器运行 `scripts/prepare_models.py`，再复制 `data/models/`。
